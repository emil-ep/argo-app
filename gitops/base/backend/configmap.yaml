apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  labels:
    app: backend
    component: api
data:
  cors.origin: "http://localhost"
  rate.limit.window: "900000"
  rate.limit.max: "100"

# Made with Bob
