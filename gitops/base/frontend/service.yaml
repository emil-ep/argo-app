apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
    component: ui
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: frontend

# Made with Bob
