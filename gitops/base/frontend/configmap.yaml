apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels:
    app: frontend
    component: ui
data:
  # Use empty string for relative URLs since frontend and backend
  # are behind the same ingress (ecommerce-dev.local:30112)
  # Frontend at / and backend at /api
  api.url: ""

# Made with Bob
